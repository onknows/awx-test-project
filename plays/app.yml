---
- name: app
  hosts: app
  become: yes

  roles:
    - { role: c2platform.core.java, tags: ["java"] }
  #  - { role: c2platform.tomcat, tags: ["tomcat"] }

  tasks:
    - block:
        - command:
            cmd: "ls {{ playbook_dir }}"
        - command:
            cmd: "ls {{ inventory_dir }}"
      delegate_to: localhost
